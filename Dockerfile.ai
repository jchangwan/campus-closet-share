# Python 3.11이 설치된 공식 이미지를 "베이스"로 사용
# slim-buster는 가벼운 리눅스(Debian Buster) 버전입니다.
FROM python:3.11-slim-buster

# 컨테이너(박스) 안에 /app 이라는 폴더를 만들고, 
# 앞으로의 모든 명령은 이 /app 폴더 안에서 실행합니다.
WORKDIR /app

# requirements.txt 파일을 박스 안으로 복사
COPY requirements.txt requirements.txt

# pip를 업그레이드하고, requirements.txt에 적힌 모든 라이브러리를 설치
# --no-cache-dir: 이미지 용량 줄여줌 
RUN pip install --no-cache-dir --upgrade pip
RUN pip install --no-cache-dir -r requirements.txt

# 내 컴퓨터의 "현재 폴더(.)"에 있는 모든 파일을 
# 컨테이너(박스) 안의 "현재 폴더(/app)"로 복사
# (.dockerignore에 적힌 파일 제외)
COPY . .

# 컨테이너가 시작될 때 자동으로 실행할 명령어
# uvicorn server:app --host 0.0.0.0 --port 8000
# --host 0.0.0.0 : 컨테이너 외부에서 접속할 수 있도록 모든 IP를 엶
# --port 8000    : 8000번 포트로 서버를 엶
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]